# Execution Engine

Purpose:
- Apply approved changes inside strict write boundaries.

RPI stage:
- Implement

Primary agents:
- executor-agent

Contracts:
- Inputs/outputs, activation rules, and write permissions are defined in contract.yaml.
